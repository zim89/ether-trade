# Accounts Service Environment Variables (.env.example)

# Application
NODE_ENV=development
ACCOUNTS_GRPC_URL=0.0.0.0:50052

# Database (PostgreSQL)
DATABASE_URL=
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=accounts_db
DB_MAX_CONNECTIONS=10
